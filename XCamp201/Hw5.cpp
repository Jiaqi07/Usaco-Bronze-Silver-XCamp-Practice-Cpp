#include <iostream>
#include <algorithm>
using namespace std;

int N, cows[1001], visited[1001] = {0};

int main(){
    cin >> N;
    int ans = 1e5;

    for(int i = 0; i < N; ++i){
        cin >> cows[i];
    }

    for(int i = 0; i < N; ++i){ //Start the pattern off with a number(0, 1, 2, 3, 4) From left to right!
        
    }

    return 0;
}
